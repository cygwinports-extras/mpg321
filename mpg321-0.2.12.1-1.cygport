my_pv="${PV[1]}.${PV[2]}.${PV[3]}-${PV[4]}"

DESCRIPTION="MP3 player, replacement for mpg123"
HOMEPAGE="http://mpg321.sourceforge.net/"
SRC_URI="mirror://sourceforge/${PN}/${PN}-${my_pv}.tar.gz"
SRC_DIR="${PN}-${my_pv}"

CYGCONF_ARGS="--with-default-audio=oss --disable-mpg123-symlink --enable-ipv6"

DOCS="README.remote"
